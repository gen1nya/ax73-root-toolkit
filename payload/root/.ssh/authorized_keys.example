# REPLACE THIS FILE WITH YOUR SSH PUBKEY BEFORE BUILDING
#
# Example:
#     cp ~/.ssh/id_ed25519.pub payload/root/.ssh/authorized_keys
#
# If you leave this placeholder, dropbear pubkey auth will not work (but
# dropbear itself also doesn't currently work on stock — see
# docs/05-dropbear-postmortem.md).
